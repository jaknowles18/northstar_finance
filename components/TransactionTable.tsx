import type { Transaction } from "@/lib/types";
export function TransactionTable({ transactions }: { transactions: Transaction[] }) { return <div className="table-wrap"><table><thead><tr><th>Merchant</th><th>Category</th><th>Date</th><th className="text-right">Amount</th></tr></thead><tbody>{transactions.map(t=><tr key={t.id}><td><div className="font-semibold">{t.normalizedMerchant}</div><div className="mt-1 text-xs text-[#8a928b]">{t.rawMerchant}</div></td><td>{t.category ? <span className="pill">{t.category}</span> : <span className="text-sm font-semibold text-amber-700">Needs category</span>}</td><td className="whitespace-nowrap text-sm text-[#626d64]">{t.transactionDate}</td><td className="text-right font-bold tabular-nums">${t.amount.toFixed(2)}</td></tr>)}</tbody></table></div> }
